<%- include ('partials/header.ejs', {titulo: titulo, usuario: usuario, login: login, sesion: sesion}) -%>

    <script>
        function validate(input) {
            let validityState_object = input.validity;

            if (validityState_object.patternMismatch) {
                input.setCustomValidity('El formato no es correcto.');
            } 
            else {
                input.setCustomValidity('');
            }
        }
    </script>

    <div class="justify-content-center align-items-center h-100">
        <div>
            <form action="/registrarAtleta" method="post">
                <h2 class="fw-bold mb-2 registrarAtletaFormTitulo">Registrar Atleta</h2>

                <!--Primera fila-->
                <div class="row" style="text-align: center">
                    <div class="form-outline form-white mb-4 col-sm registrarAtletaFormInput">
                        <label class="form-label fw-bold" for="nombreCompleto">Nombre Completo</label>
                        <input id="nombreCompleto" name="nombre" class="form-control" />
                    </div>
                    <div class="form-outline form-white mb-4 col-sm registrarAtletaFormInput">
                        <label class="form-label fw-bold" for="Altura">Altura</label>
                        <input id="Altura" name="altura" class="form-control" pattern="^[0-3]\.[0-9]{2}$"
                        onsubmit="validate (this)" />
                    </div>
                    <div class="form-outline form-white mb-4 col-sm registrarAtletaFormInput">
                        <label class="form-label fw-bold" for="Peso">Peso</label>
                        <input id="Peso" name="peso" class="form-control" pattern="^[0-9]{1,4}\.{0,1}[0-9]{1,10}$"
                        onsubmit="validate (this)" />
                    </div>
                </div>

                <!--Segunda fila-->
                <div class="row" style="text-align: center">
                    <div class="form-outline form-white mb-4 col-sm registrarAtletaFormInput">
                        <label class="form-label fw-bold" name="fechaNacimientoLabel" for="fechaNacimiento">Fecha de
                            Nacimiento</label>
                        <input id="fechaNacimiento" name="fechaNacimiento" class="form-control" type="date" />
                    </div>

                    <div class="form-outline form-white mb-4 col-sm registrarAtletaFormInput">
                        <label class="form-label fw-bold" for="CURP">CURP</label>
                        <input
                            id="CURP"
                            name="curp"
                            class="form-control"
                            pattern="^[A-Z]{1}[AEIOU]{1}[A-Z]{2}[0-9]{2}(0[1-9]|1[0-2])(0[1-9]|1[0-9]|2[0-9]|3[0-1])[HM]{1}(AS|BC|BS|CC|CS|CH|CL|CM|DF|DG|GT|GR|HG|JC|MC|MN|MS|NT|NL|OC|PL|QT|QR|SP|SL|SR|TC|TS|TL|VZ|YN|ZS|NE)[B-DF-HJ-NP-TV-Z]{3}[0-9A-Z]{1}[0-9]{1}$"
                            onsubmit="validate (this)"
                            required
                        />
                    </div>
                </div>
                <!--Tercera fila-->
                <div class="row" style="text-align: center">

                    <div class="form-outline form-white mb-4 col-sm registrarEmpleadoFormInput">
                        <label class="form-label fw-bold" for="telefono">Teléfono</label>
                        <input
                            id="telefono"
                            name="telefono"
                            class="form-control"
                            pattern="^\+{0,1}[0-9]{10,12}$"
                            onsubmit="validate (this)"
                        />
                    </div>

                    <div class="form-outline form-white mb-4 col-sm registrarAtletaFormInput">
                        <label class="form-label fw-bold" for="membresia">Membresia</label>
                        <select id="membresia" name="membresia" class="form-select">
                            <option value="" selected hidden></option>
                            <option value="Semanal">Semanal</option>
                            <option value="Mensual">Mensual</option>
                            <option value="Anual">Anual</option>
                        </select>
                    </div>

                </div>
                <!--Botones-->
                <div style="text-align: center; margin-top: 25px">
                    <button type="submit" class="btn btn-dark">Registrar</button>
                </div>
            </form>
        </div>

        <div style="text-align: center; padding-top: 21px; font-weight: bold;">
            <!-- Rellenar dinamicamente con mensaje de error -->
            <p>
                <%= mensajeError %>
            </p>
        </div>
    </div>

    <%- include ('partials/footer.ejs') -%>